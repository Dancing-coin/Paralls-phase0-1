# 八月分析实现收口与证据映射

状态：`audited 2026-08-29; bounded implementation closure, not a production-completeness claim`

本文是八月分析目录的状态收口页。它不替代设计内容，也不新增实现授权；它只把设计文档
映射到当前代码、测试和 Harness 证据。状态含义如下：

- `已实现`：当前范围有代码和可复现验证证据。
- `部分实现`：有可复用基础或窄垂直切片，但设计描述的完整域仍未完成。
- `未实现/后置`：当前只有设计指导，没有对应的实现闭环证据。
- `已实现但证据包需补齐`：功能代码和聚合验证存在，但缺少该文档要求的独立报告或运行时证明。

## 证据基线

### 2026-08-28 Fresh INF/Mainline Baseline

The current repository evidence supersedes the historical 2026-08-12 probe:

```text
INF-focused corpus                                      current 1231 passed
repository-root pytest                                  4001 passed
INF-4AI focused/catalog suite                           34 passed
INF-4AI independent Harness                             PASS
INF continuation gate                                   PASS
docs Harness                                             PASS
compileall / git diff --check                           PASS
```

The repository `all` Harness remains externally bounded at
`siming-heavenly-runtime`: heavenly mode, endpoint, model, and API key are
unavailable, so no live call was attempted. This is an environment
precondition, not an INF code failure and not a full-Harness green claim.

The INF ledger now contains verified narrow rows across Construction,
Economy, Ecology, Survival, Government, Organization, Contract, Ownership,
and embodied foundations. These rows retain independent owner, privacy,
revision, idempotency, receipt, zero-write, and replay evidence. They do not
complete the broader August products: generic payment/transfer/settlement,
generic transform/promotion, full population/social/group truth, creator
control-plane delivery, real provider production integration, and civilization
or supernatural systems remain partial, blocked, or post-poned.

2026-08-12 重跑结果：

```text
python -m pytest -q                                      2451 passed
phase1d-econ1-bakery                                    PASS
phase1e-generalization-gate                             PASS
phase2-bakery-authored-agents                           historical PASS; invalidated (direct-batch sample)
p2dr-authored-bakery-authority-reclosure                PASS (bounded owner-driven re-closure)
phase3-population-continuity                            PASS
phase4d-commercial-ecosystem                            PASS
phase5d-investigation-vertical-slice                    PASS (66 focused tests)
```

这些 profile 证明的是限定范围的后端 authority、事件、回放、权限和零写入边界；只有 P1D
报告包含 committed Godot mirror 证明，后续 P2-P5 profile 主要是后端/投影验证，不能据此
宣称完整的生产级 Godot 联机闭环。

## 根目录文件

| 文档 | 对应实现情况 | 证据/边界 |
| --- | --- | --- |
| `00-增量设计工作台与覆盖矩阵.md` | 已实现为治理索引 | 正式 spec/plan 与 Harness 是状态来源；本文件是其收口补充。 |
| `01-项目定位与当前能力边界.md` | 已实现部分定位 | 当前是角色智能体、世界权威、可回放玩法底座；不是完整 RPG、创作者平台或世界模型产品。 |
| `02-角色档案与状态核心架构.md` | 已实现 | Character Core/L1-L4 和既有 CharacterProfile 被 P2/P3/P5 复用；完整闭源产品化未完成。 |
| `03-玩法底座与经济跨域协作.md` | 已实现限定域 | P1-P5 已有单一 authority、事件和回放闭环；通用跨域玩法仍需扩展。 |
| `04-VLA世界模型与机器人沙盒方向.md` | 部分实现 | 有 VLA/provider/advisory 接口基础；没有生产级世界模型或机器人闭环。 |
| `05-多呈现形态底座与具身交互.md` | 部分实现 | Godot mirror、结构化行动和有限物理切片存在；多形态生产交付、archive-door 完整成功路径未闭合。 |
| `06-创作者工具Codex控制与生态商业化.md` | 未实现/后置 | 创作者 CLI/MCP、资产市场、内容发布和分账不是 P1-P5 交付物，对应 P6/后续路线。 |
| `07-算力成本记忆复用与运行经济.md` | 未实现/后置 | 有记忆、回放和降级基础；计费、缓存市场和生产成本控制没有运行时闭环。 |
| `08-未来底座产品架构与演进路线.md` | 路线指导 | 产品族和长期路线仍是规划；P1-P5 只完成其中的 RPG/经营/社交窄切片。 |
| `09-线程结论与决策清单.md` | 已实现部分结论 | owner、事件、回放、权限和“不伪造 NPC”结论已被验证；长期 backlog 仍未授权。 |
| `10-创作者权限与闭源控制面方案.md` | 未实现/后置 | 现有权限过滤不能等同于 CreatorControlPlane、发布审批或生产 revision 管理。 |
| `11-全面闭源核心保护与创作者控制面方案.md` | 部分满足边界，功能未实现 | 后端 authority/Godot presentation 边界已存在；完整闭源部署、签名包、控制面未实现。 |

## 世界基础设施增量指导

| 文档 | 对应实现情况 | 说明 |
| --- | --- | --- |
| `00-标签体系与元规则引擎.md` | 部分实现（INF-1、INF-1R、INF-1X production-only 与 INF-2X construction lifecycle row 已验证） | `semantic_registry.py` 现有 TagDefinition/Assignment、继承、参数冲突拒绝、snapshot digest、selector，以及只针对 `effect:production_due_finish` 的 closed RuleSet/effect/resistance/owner mapping。`SemanticSettlementAuthority` 仅经 `ConstructionProductionAuthority.build_due_finish_fragment` 写入既有 event spine；INF-2X 只注册 construction v1 settled/cancelled owner events，故通用 state lifecycle 与全域 meta-rule executor 未实现。Harness：`infra-semantic-entity-causal`、`infra-semantic-cross-domain`、`infra-general-semantic-rule`、`infra-obligation-lifecycle`。 |
| `12-时间调度跨域结算与回放.md` | 部分实现（INF-2、INF-2R 与 INF-2X construction lifecycle row 已验证） | `backend/app/world_runtime/simulation_clock.py` 提供 caller-driven tick/due/catch-up/rewind gate；`ConstructionDueCompletionPolicy@1` 通过 registered construction owner fragment 追加 settled/cancelled correlation events。retry、failure、compensation、生态和其他 owner rows 仍明确 zero-write reject。Harness：`infra-time-obligation`、`infra-multi-domain-obligation`、`infra-obligation-lifecycle`。 |
| `13-实体档案语义因果与元规则.md` | 部分实现（INF-1 已验证） | `entity_causal_projection.py` 从 committed events 导出 Entity/Thing/Environment/Relationship/CausalEvent dossier，含父子因果查询和 checkpoint-tail 等价；跨域 effect/settlement 仍未完成。Harness：`infra-semantic-entity-causal`。 |
| `14-群体模拟世界模式与文明演进.md` | 已实现限定切片 | P3A-P3D 实现了 profile-backed activation、连续性和面包店街区样板；文明演进、跨区人口和长期模拟未实现。 |
| `18-生态环境与灾害系统.md` | 已实现限定域 | INF-3 verifies finite weather-front propagation and exact drought/rain consumer rows; long-horizon ecology, generic fanout, and broad disaster simulation remain unimplemented. |

## 玩法系统

| 文档 | 对应实现情况 | 说明 |
| --- | --- | --- |
| `01-玩法系统总纲.md` | 已实现限定域 | 单一 canonical owner、typed intent、authority settlement、replay 和 zero-write 已贯穿 P1-P5。 |
| `02-行动基元与具身交互.md` | 部分实现 | 结构化行动、affordance、attestation、settlement 和有限 archive-door 切片存在；默认场景全覆盖和完整物理动作未完成。 |
| `03-技能与成长系统.md` | 部分实现 | skill/ability/affordance 与资格检查可用；完整成长、训练和长期 progression 未完成。 |
| `04-背包装备与产权系统.md` | 已实现限定域 | inventory、container、encumbrance、equipment、ownership、custody 和回放已在 P1/P2 使用；通用物品生态仍有限。 |
| `05-建造生产与设施系统.md` | 已实现限定域 | P1 Econ-1 plus INF-1 repair, facility transforms, lifecycle, operational verification, public-use, and project-step rows are verified; generic Construction action/transform remains blocked. |
| `06-生存需求与角色状态系统.md` | 已实现限定域 | survival profile、need/consumption proposal、工资/经营窗口与过期义务已有验证；连续世界需求模拟仍有限。 |
| `07-任务目标证据与剧情系统.md` | 已实现限定域 | P5A/P5D 有 quest objective、evidence provenance、hidden clue privacy、objective transition 和 replay。 |
| `08-战斗冲突与风险系统.md` | 部分实现 | P5C/P5D 有调查、潜行、报警、非致命冲突和 adverse outcome；不是完整战斗、伤害或战争系统。 |
| `17-修炼超自然与多资源系统.md` | 未实现/后置 | 可复用 resource/body/status/skill 原语存在，但修炼、超自然和多资源玩法没有闭环证据。 |

### 社会与制度玩法

| 文档 | 对应实现情况 | 说明 |
| --- | --- | --- |
| `02-经济系统底座.md` | 已实现限定域 | P1 fixed quote/account/debt/contract/tax 与 P4 dynamic quote/clearing/credit 已验证；宏观金融和自由市场未实现。 |
| `03-模拟经营完整玩法与多智能体协作.md` | 已实现限定切片 | P1/P2 面包店和 P3 人口连续性样板已存在；通用顾客/供应商/监管人口生活模拟仍后置。 |
| `10-商业组织经营系统.md` | 已实现限定域 | P2 工作、工资、经营窗口和 P4 多组织商业生态存在；通用组织生命周期和破产系统仍有限。 |
| `11-政府组织与监管系统.md` | 已实现限定域 | permit、inspection、tax、public obligation、credit/revision rejection 已验证；通用政策激活和申诉系统未完成。 |
| `21-金融信用与投资系统.md` | 部分实现 | P4 有有限信用、债务、公共义务和确定性结算；投资、跨区清算、系统性金融未实现。 |
| `22-政治军事与公共秩序系统.md` | 部分实现 | P4/P5 保留公共秩序、监管和冲突边界；政治、军事、领土和战争模拟未实现。 |
| `23-文明能力与六轴传导.md` | 未实现/后置 | 属于 P7/长期研究方向，当前没有文明能力或六轴因果传导闭环。 |

## 收口规则

1. 以后阶段状态以正式 `docs/superpowers/specs/...`、`docs/superpowers/plans/...` 和最新 Harness 报告为准。
2. 本目录的设计文档只描述边界和目标；不能把 `reusable` 原语写成完整域已完成。
3. `P3 population continuity` 现在已有实现和验证，但仍只代表受限人口连续性切片，不代表完整 Population Simulation 或文明模拟。
4. P5 当前有 P5D 聚合 focused 证据；若要宣称 P5A/P5B/P5C 各自独立收口，应重新生成对应独立报告。
5. 未经 Godot runtime 或生产连接验证，不得把后端 projection checksum 写成完整 Godot 联机完成。

## 2026-08-27 INF A-D Boundary Reconciliation

August INF A-D remains `not complete`. The verified narrow ledger currently
includes, among others, INF-1AH Construction decommission, INF-1AI/1AJ/1AK
Construction follow-on rows, INF-2AD/2AE/2AF/2AG Economy/Contract rows,
INF-3R/3S/3T/3U/3V Ecology/Government/Contract/Survival rows, and
INF-4U/4V/4W/4AG/4AH/4AI Ownership/Organization/Government/Social rows.
INF-P is a platform prerequisite and is not substituted for August INF
completion.

The ordered continuation scan is still active. Remaining INF-1 shapes are
duplicate/closed or missing target semantics; INF-2 Slots B/C and generic
settlement lack distinct business tuples; INF-3 has no additional admitted
target-owner edge; and INF-4 broad branch, participant, attendance,
population, social, and group-truth outcomes lack committed domain facts or
platform expression. The autonomous row-resolution mandate allows ordinary
implementation/content repair and strictly row-specific upstream facts under
existing owners, but never permits defaults, fixture inference, generic
writers, or a second runtime/store. These are row-level dispositions, not a
Goal-level block.

The latest INF-4 candidate review formed INF-4AI's exact implemented narrow
row: completed mutual handshake -> two actor-private shared-experience facts
through the existing SocialFactAuthority. The former P5/schema and catalog
expression blocker is historical and superseded by the verified static
vocabulary, closed actor-private scope, focused tests and independent Harness.
Generic social, attendance, population and group outcomes remain row-level
blocked or unimplemented; August completion status is unchanged.

The repository `all` Harness remains locally green except for the external
`siming-heavenly-runtime` preflight, which cannot run without heavenly mode,
online endpoint/model, and API key. No live call was attempted; this is an
environment limitation, not an INF code failure. Goal remains `active` and
August INF A-D remains `not complete`.

### 2026-08-28 INF-2AI Budget Consumption Closure

The exact completed public-workshop activity plus matching fixed public-project
budget reservation now produces one Economy-owned authority-only
`public_project_budget_consumed@1` marker. Focused tests and the independent
Harness are green. This is a consumed marker only: no debit, release, refund,
transfer, compensation or generic budget lifecycle is admitted. Slots B/C and
generic payment remain blocked.

### 2026-08-28 INF-4AJ / INF-4AK Project Closure Boundaries

The exact project-visible INF-4AG activity plus authority-only INF-2AI consumed
budget now yields one Organization-owned `public_project_execution_recorded@1`
fact with fixed `funded_and_executed` semantics. No payment, material, output,
attendance, social, population, or generic project lifecycle is admitted.

The subsequent INF-2AK row records only the matching authority-only terminal
budget close marker. INF-4AK then records one authority-only Government
administrative acknowledgment from the exact funded execution and its
Economy/acquisition provenance. Neither row admits payment, transfer, release,
refund, permit, certificate, material, output, attendance, social, population,
or generic project lifecycle semantics.

### 2026-08-29 INF Ordered Row Refresh

The current mainline ledger additionally verifies INF-1AM fixed mill-flour
output certification, INF-2AM certified-flour custody plus v7 purchase,
INF-3 grain harvest and fixed Inventory custody, and INF-4AO/INF-4AP milling
notice acknowledgment and organization grain intake. Each row uses existing
owners and append-derived receipts with privacy, revision, idempotency and
full/checkpoint-tail replay evidence. Remaining generic/unlisted rows retain
their formal blockers; August INF A-D remains `not complete`.

Next minimum decisions are explicit: Construction facility binding for grain
intake, Economy buyer/account/price binding, a new committed Ecology
source-target edge, or Government/Social jurisdiction/participant truth. No
default or generic authority is implied.

### 2026-08-29 Current Evidence Refresh

The current INF/INFRA keyword selection is `1339 passed` with `2758
deselected`; the latest repository-root suite is `4093 passed`. The local
Harness inventory contains 166 versioned profiles and 166 matching reports,
all with `overall_passed=true`. This validates the existing narrow rows and
boundary checks only; the four residual lane dispositions remain row-level
blockers and August INF A-D remains `not complete`.

INF-2AN's exact Organization grain-intake -> Economy acceptance-marker row is
now included in the verified narrow ledger. It records acceptance only and
does not imply debit, credit, payment, transfer, pricing, or generic
settlement.

### 2026-08-29 Closed Generic Gameplay Foundation v1 Matrix

The separate refactoring Goal now has a source-controlled twelve-family
matrix and independent Harness report at
`.harness/verification/closed-generic-gameplay-families-report.json`.
`recipe_production@1`, `facility_identity_upgrade@1`,
`production_output_certification@1`, `fixed_service_exchange@1`,
`declared_exchange@1`, and `owner_bound_environment_consumer@1` have
two-content genericity evidence through immutable descriptor/catalog admission,
exact-one binding, owner-bound source/privacy/revision checks, idempotency,
append provenance, duplicate replay, and the existing append spine. The other
five writable families have dedicated bounded adapters, focused tests,
Harnesses, and explicit genericity blockers. `production_output_custody@1` is
explicitly blocked:
the committed facts do not yet provide output quantity, source-to-holder
mapping, or a unique destination container. No caller/default inference is
allowed. This foundation remains independent from August INF A-D, which is
still `not complete`.

### 2026-08-30 Generic Family Admission-Digest Refresh

The family binding helper is now exercised by the real patch activation path:
typed family content and declaration payload digests are recomputed before an
active-set mutation. The closed-family aggregate report records the committed
manifest paths supporting each of the six currently generic families. This is
admission evidence only; the matrix remains `6 generic / 5 bounded / 1
blocked`, and no bounded row is promoted without a second committed content
instance plus family-specific source and replay proof. The custody blocker and
August INF A-D `not complete` boundary are unchanged.

### 2026-08-31 Foundation Matrix Closure Decision

Under the approved completion definition, the foundation matrix is complete at
the governance level when each family is either implemented and verified or
formally blocked with zero-write evidence. The current status is therefore
`11 generic / 0 bounded / 1 blocked`: all writable families have multi-content
proof, while `production_output_custody@1` remains blocked by missing committed
quantity, holder mapping, and unique destination-container facts.
`generic_refactoring_complete=false` remains intentional because the blocked
family is not a generic writer. This does not complete August INF A-D, which
remains `not complete`.
