# F1A 语义规则因果与调度门

状态：`partial-foundation verified; generic contract remains planned`

## 大白话

世界基础设施那一堆“标签、实体、性质、效应、因果、时间、世界模式”不能继续靠字符串和各自理解。F1A 要先定共同提案格式和版本规则，但仍然回到已有 Gameplay authority 结算，不造第二个世界循环。

## 必须定义

- schema/digest、owner、依赖、capability budget、输入/生效 revision；
- 冲突、循环依赖、过期 revision、未授权能力的确定性拒绝；
- 因果引用和解释投影；
- 跨域时间请求怎样通过现有 settlement 路径处理。

## 验收

focused tests 要覆盖 accept/reject、stale/cycle/conflict denial、因果 trace、重复幂等、full/checkpoint-tail replay、解释投影和拒绝零写入。未来实现必须使用 `GameplayEventStore.append_batch()`，不能另建 scheduler、clock 或 Rule IR 执行器。

对应 formal spec：[F1A design](../../superpowers/specs/world-character-siming-authority-mainline/post-p5-capability-foundation/2026-08-12-f1a-semantic-rule-and-causal-extension-gate-design.md)。窄基础证据：`.harness/verification/post-p5-f1a-foundation-report.json`。
