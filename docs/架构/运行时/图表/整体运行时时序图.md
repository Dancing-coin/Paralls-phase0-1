# 运行时时序图

状态：当前运行时图表文档

这些图描述当前已验证的运行时路径，不是未来产品理想态。配合下面文档使用：

- `docs/架构/运行时/模块/SystemL1.md`
- `docs/架构/运行时/模块/角色智能体.md`
- `docs/架构/运行时/运行时覆盖矩阵.md`

## 对话循环

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Godot as Godot 主 Demo/玩家意图
    participant Bridge as BackendBridge WebSocket
    participant Main as backend/app/main.py
    participant Character as 角色服务/角色智能体运行时
    participant Bus as System L6 AuthorityEventBus
    participant Projection as FrontendAuthorityEventProjector
    participant Presentation as 本地表现总线/角色副本

    Player->>Godot: 提交 dialogue intent
    Godot->>Bridge: player_input(dialogue_submit)
    Bridge->>Main: WebSocket envelope
    Main->>Character: 路由结构化 dialogue
    Character-->>Main: dialogue_response + 运行时回写
    Main->>Bus: dialogue / runtime AuthorityEvent
    Bus->>Projection: AuthorityEvents
    Projection-->>Presentation: 前端兼容 projection
    Main-->>Bridge: dialogue_response
    Bridge-->>Presentation: backend_message / signal
    Presentation-->>Player: 可见回应 + voice stub 证明
```

验证：

- `python scripts/verification/harness.py --profile phase0`

## 交互成功

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Godot as Godot 主 Demo/对象焦点
    participant L1 as System L1 事实
    participant Main as backend/app/main.py
    participant ESM as ESMService
    participant Authority as System L6 AuthorityEventBus
    participant Projection as FrontendAuthorityEventProjector
    participant GodotWorld as 对象/环境表现
    participant Siming as SimingRuntime

    Player->>Godot: 对焦点对象发起 interact
    Godot->>L1: raw_fact_event + interact_intent
    L1->>Main: WebSocket 结构化 envelopes
    Main->>ESM: 语义交互结算
    ESM-->>Main: 成功的 world/object/environment/body results
    Main->>Authority: 发布 AuthorityEvent result family
    Authority->>Siming: 消费 authority event
    Siming-->>Authority: 高层 catalyst candidate/output
    Authority->>Projection: AuthorityEvents for frontend projection
    Projection-->>GodotWorld: world_result / state_machine_transition / siming_output
    GodotWorld-->>Player: 可见状态变化与反应
```

验证：

- `python scripts/verification/harness.py --profile phase0`
- `python scripts/verification/harness.py --profile interaction-orchestration-service`

## 交互失败 / 约束

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Godot as Godot 主 Demo/对象焦点
    participant Main as backend/app/main.py
    participant Orchestration as InteractionOrchestrationService
    participant ESM as ESMService
    participant Authority as System L6 AuthorityEventBus
    participant Projection as FrontendAuthorityEventProjector
    participant Presentation as Godot UI/对象状态

    Player->>Godot: 距离不足或被阻挡时 interact
    Godot->>Main: structured interact_intent
    Main->>Orchestration: 选择 semantic/physical policy
    Orchestration->>ESM: 请求 authority settlement
    ESM-->>Orchestration: constraint_state_result
    Orchestration-->>Main: unified denied/degraded result
    Main->>Authority: 发布 constraint AuthorityEvent
    Authority->>Projection: AuthorityEvents for frontend projection
    Projection-->>Presentation: world_result / constraint feedback
    Presentation-->>Player: 可观察的失败交互
```

验证：

- `python scripts/verification/harness.py --profile phase0`
- `python scripts/verification/harness.py --profile esm-physical-channel-world-actuation`

## Siming 催化反应

```mermaid
sequenceDiagram
    participant L1 as L1/公开事实
    participant Authority as System L6 Authority 事件总线
    participant Situation as SimingGlobalSituationLayer
    participant Siming as SimingRuntime/事件管线
    participant Character as CharacterAgentRuntime
    participant Main as backend/app/main.py
    participant Bridge as BackendBridge/本地表现总线
    participant Projection as FrontendAuthorityEventProjector
    participant Godot as Godot 角色副本/观测面

    L1->>Authority: visual/world/conversation/constraint AuthorityEvents
    Authority->>Situation: 公开 world 与 evidence refs
    Situation-->>Siming: fairness/global situation snapshot
    Siming->>Siming: candidate/policy/feasibility/audit
    Siming-->>Authority: siming.* 高层 AuthorityEvent
    Authority->>Character: 高层 catalyst input 适配，不是 character_agent_execution 投递路径
    Character-->>Main: character_agent_execution envelope
    Main-->>Bridge: WebSocket message
    Bridge-->>Godot: character_agent_execution_received
    Authority->>Projection: 前端兼容投影
    Projection-->>Godot: siming_output / debug event / script beat
    Godot-->>Godot: attention、observatory 或表现反应
```

说明：`Authority -> Character` 只表示 `siming.*` 高层事件被适配为角色可消费的 catalyst input。
`character_agent_execution` 仍由 `CharacterAgentRuntime -> backend/app/main.py -> BackendBridge -> Godot` 投递，不走 L6。

验证：

- `python scripts/verification/harness.py --profile phase0`
- `python scripts/verification/harness.py --profile siming-global-situation-layer`

## 角色智能体执行投递

```mermaid
sequenceDiagram
    participant L1 as CharacterPerceived/SelfBody/Siming 输入
    participant Runtime as CharacterAgentRuntime
    participant L2 as L2 推理器
    participant L3 as L3 规划器
    participant L4 as L4Executor
    participant Main as backend/app/main.py
    participant Bridge as BackendBridge/本地表现总线
    participant Actor as 角色副本/运行时状态
    participant Skin as KnightRoleSkin

    L1->>Runtime: actor-private input
    Runtime->>Runtime: timeline + memory 回写
    Runtime->>L2: private snapshot + memory state
    L2-->>Runtime: interpretation
    Runtime->>L3: interpreted reality + goal/control mode
    L3-->>Runtime: selected intent / suggestions
    Runtime->>L4: execution request
    L4-->>Runtime: 五通道 execution plan
    Runtime-->>Main: character_agent_execution envelope
    Main-->>Bridge: WebSocket message
    Bridge-->>Actor: character_agent_execution_received
    Actor->>Actor: AgentControllerAdapter + CharacterRuntimeState
    Actor->>Skin: CharacterPresentationInput-shaped data
    Skin-->>Actor: 本地 embodiment/presentation
```

验证：

- `python scripts/verification/harness.py --profile character-agent-execution`
- `python scripts/verification/harness.py --profile phase0`

## Godot Provider 到 PQF

```mermaid
sequenceDiagram
    participant Godot as Godot Provider 节点
    participant Bridge as BackendBridge
    participant Main as backend/app/main.py
    participant L1Bridge as L1RuntimePerceptionBridge
    participant PQF as PerceptionQueryFrame
    participant Character as CharacterAgentRuntime
    participant Siming as SimingRuntime

    Godot->>Godot: 采样 visual/spatial/auditory/body/skeletal/environment refs
    Godot->>Bridge: provider refs / raw_fact_event
    Bridge->>Main: WebSocket envelope
    Main->>L1Bridge: 组装面向运行时的 perception refs
    L1Bridge-->>PQF: schema-valid query frame
    PQF-->>Character: character_mm scoped bundle
    PQF-->>Siming: siming_mm scoped bundle
```

验证：

- `python scripts/verification/harness.py --profile godot-sampling-production-grade-providers`
- `python scripts/verification/harness.py --profile l1-world-fact-runtime`
