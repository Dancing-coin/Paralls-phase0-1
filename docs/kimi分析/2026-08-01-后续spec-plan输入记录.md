# 后续 spec / plan 输入记录

- 日期：2026-08-03
- 状态：已复核更新
- 作者：Codex
- 证据基准：基于 2026-08-03 对当前工作区代码、活跃正式 spec / plan、最新 `.harness/verification/` 报告和本线程讨论的综合核对
- 性质：后续正式规格树与实施计划的输入记录，不替代正式 spec / plan
- 治理说明：本文件只记录当前讨论中已经明确且对后续建模有约束力的信息；若后续代码事实变化，应在正式 spec / plan 中优先反映新事实

## 1. 本文件的用途

本文件用于给后续新线程写正式 spec / plan 时提供稳定输入，避免重复提问、重复判断和把已明确的方向再次反复讨论。

后续新线程在开始写文档前，必须先阅读本文件，再对照当前代码事实做一次快速复核。

## 2.7 2026-08-03 活跃 plan / spec 复核结论

当前仓库的活跃执行面已经从单纯的角色智能体和 VLA 方案讨论，推进到
`world-character-siming-authority` 主线下的 gameplay foundation、WebSocket
session identity、Godot gameplay mirror 和默认场景具身交互覆盖。

当前最重要的判断不是“所有主线都已完成”，而是：

- world runtime、actor-local perception、social contact、authority settlement、
  execution ingress 和 continuity 已有主线聚合证据；
- Gameplay event store / authority bus shared spine 已实现并验证；
- `CharacterGameRuntimeState`、state groups、resource/status/effective-stats、
  inventory/equipment/economy、Patch runtime 已形成多个最小 backend slice，
  但不能表述为完整玩法底座；
- WebSocket session binding、scope filtering、mirror source/fanout 已有基础，
  但 reconnect/resync 和真实 Godot live mirror delivery 仍开放；
- 默认主场景目前是 reviewed reference fixtures 的逐步覆盖，不是全场景对象
  自动具身化；
- 最新 Harness 聚合报告为通过，但它证明的是已注册 profile 的 bounded
  contract/runtime slices，不等于所有后续 closure 已完成。

完整进度分析见：

- `docs/kimi分析/2026-08-03-活跃plan与spec进度分析.md`

## 2. 已明确的产品方向

### 2.1 总目标

目标不是继续堆局部 demo 能力，而是让项目逐步具备：

- 更一致的智能体自由度
- 更强的具身交互可信度
- 更自然的说话与发声体验
- 更清晰的玩法状态底座

大白话说：

- 不能只让智能体在少数热点对象上“很聪明”
- 要让它在越来越多默认对象、默认动作和默认交互里都遵守同一套规则

### 2.2 VLA 当前产品决策

已明确：

- `VLA deep` 当前**暂时搁置**
- 原因：`plus` 模型推理速度偏慢，不适合当前阶段的在线反应目标
- 当前阶段应把 VLA 视为：
  - `fast-only online advisory`
  - 以较快反应优先
  - 不把 `deep` 当当前 blocker

VLA 的角色仍然必须保持：

- advisory provider
- 不写 `world truth`
- 不写 `ESM authority`
- 不直接控制角色动作
- 已知场景真值优先来自 Godot scene/entity/collider/anchor/registry

### 2.3 VLA 模型选择方向

对当前项目的在线 VLA 路线，当前讨论已明确建议：

- 默认优先：`qwen3.7-flash`
- `qwen3.7-plus` 作为未来可恢复的更慢更深层路线，而不是当前默认目标

如果后续正式 spec / plan 仍保留 `deep` 概念，必须明确写成：

- deferred
- parked
- non-blocking

而不是继续把它写成当前阶段必须收口的硬要求。

### 2.4 TTS 当前产品方向

已明确：

- TTS 是表现层，不是文本模型替代物
- TTS 的目标是让角色“像活着一样开口说话”
- 它应与 `dialogue streaming` 联动

接入策略优先级：

1. 真实 TTS provider
2. 句子级 / 分段流式语音
3. 真流式音频作为后续增强

### 2.5 动作库 / 原子动作方向

已明确不采用纯一边倒路线。

推荐正式方向：

- **控制器主导，动作库供给**

具体边界：

- 控制器负责：
  - 去哪里
  - 站在哪里
  - 什么时候接触
  - 是否中断
  - 如何恢复
- 动作库负责：
  - 原子动作资产
  - root motion profile
  - modifier / overlay
  - equipment override
  - 可组合的表现性动作片段

### 2.6 root motion 使用原则

已明确：

- root motion 不拥有整段空间真相
- 大位移由控制器 / navigation / stance reservation 决定
- root motion 主要负责：
  - 局部位移
  - 身体节奏
  - 接触前后表达
- IK / motion warping 用于贴合目标
- authority 决定最终结果

## 3. 动作库正式设计应保留的结构

### 3.1 三层结构

后续正式 spec / plan 应优先延续这三层：

1. **语义动作层**
   - 例如：`approach`、`align`、`kick`、`grab`、`place`、`handoff`
2. **原子动作层**
   - 例如：起步、停步、转身、伸手、握持、踢击、收手、恢复
3. **本地执行层**
   - `EmbodiedActionController` / `CharacterMotor` / IK / motion warping / local observation

### 3.2 原子动作分类建议

后续正式动作库至少要按以下类别组织：

- 位移动作原子
  - `start_move`
  - `stop_move`
  - `turn_to_target`
  - `step_left`
  - `step_right`
  - `backstep`
- 上肢原子
  - `raise_hand`
  - `reach_forward`
  - `grip`
  - `release`
  - `offer_item`
  - `receive_item`
- 接触原子
  - `kick_contact`
  - `push_contact`
  - `tap_contact`
  - `brace_contact`
- 恢复原子
  - `recover_balance`
  - `reset_guard`
  - `return_idle`
  - `abort_contact`

### 3.3 资产 contract

动作资产 contract 继续沿用当前方向：

- `CharacterActionAssetDescriptor`
- `CharacterEmbodimentAssetRegistry`
- `selected_skill_path`
- `primitive_action_tags`
- `primitive_realization_keys`

不要新造第二套并行资产 contract。

## 4. 当前代码事实（对后续 spec / plan 重要）

### 4.1 已经完成并验证

- 角色 `dialogue` / `L2` / `L3` 的 gateway、validator 和 live-proof 代码路径
- 角色历史 DeepSeek live proof 产物；后续 closure 仍必须使用同一新鲜 run ID 重新确认
- Siming 的 policy / feasibility / producer / AuthorityEventBus 组件链
- `dialogue streaming`
- 真实 TTS provider + Godot 播放
- VLA fast live provider 路线
- 具身交互 foundation 第一批闭环：
  - affordance registry
  - bridge attestation
  - controller
  - authority settlement
  - replay
  - interaction session
  - carry / handoff slices
- Gameplay event store、atomic event batch、replay、committed outbox 和 authority
  bus shared spine
- `CharacterGameRuntimeState` 的最小只读组合核心和 StateGroupRegistry 的基础切片

### 4.2 已完成但仍在深化

- TTS voice profile adapter
- VLA 双层路由中的 `deep` 分层语义；当前生产路线固定为 fast-only，deep parked
- 技能系统从 shadow 到 bounded behavior consumption 后的更深层能力图谱
- WebSocket session identity、scoped mirror read path 和 after-commit fanout
- Patch 的首个 typed resource data-transform migration slice
- 默认主场景中 reviewed object affordance、atomic action 和 custody/stow/retrieve
  的窄切片

### 4.3 仍未真正实现

- 完整 gameplay facade 和跨 domain 的 inventory / ownership / economy /
  equipment closure
- 通用 Patch data migration、跨版本 rollback/compensation 和生产级 registry
- WebSocket reconnect/resync、重新 enrollment、真实 Godot gameplay mirror live
  delivery
- 原子动作的完整播放、CharacterMotor/IK/motion-warping 接线和 Wave 2/3 场景覆盖
- 全场景默认对象的全面具身交互铺开
- 新鲜、同一 run ID 绑定的 Character + Siming LLM closure；旧报告不能直接作为
  当前完成证据
- 句级 TTS clip queue，以及 VLA deep 的重新准入和多场景人工 annotation review

## 5. 后续 spec / plan 不应再重复犯的错误

后续新线程写正式 spec / plan 时，不应再：

- 把 `VLA deep` 继续写成当前阶段硬 blocker
- 把 TTS 当成文本模型替代物
- 把 root motion 写成空间真相拥有者
- 把动作库写成执行控制器
- 把控制器写成动作库替代物
- 把 `CharacterGameRuntimeState` 描述成已实现事实
- 把已经有正式 spec / plan 的主题重新塞回 `docs/kimi分析`

## 6. 后续正式 spec / plan 应优先处理的主题

按当前讨论，后续应优先整理或推进：

1. 原子动作库与本地控制器协同设计
2. VLA `fast-only` 当前阶段正式化
3. TTS / dialogue streaming / voice profile 协同边界
4. WebSocket session scope 到 Godot mirror 的 reconnect/resync/live delivery
5. 默认主场景对象的具身交互覆盖路线
6. `CharacterGameRuntimeState` 最小可执行核心和 gameplay domain closure
7. LLM closure 证据的新鲜度、run ID 和真实 provider 配置收口

## 7. 建议新线程使用方式

新线程开始时应：

1. 先阅读本文件
2. 再核对当前代码事实
3. 然后决定：
   - 是写新的正式 spec / plan
   - 还是更新已有正式 spec / plan
4. 明确区分：
   - 当前阶段产品目标
   - 已推迟目标
   - 长期保留目标

## 8. 备注

本文件不保存：

- 真实 API key
- 不应入库的运行密钥
- 临时命令历史

后续正式 spec / plan 只能引用变量名和能力边界，不能把 secret 写入版本库。
