# P6D Creator Operations Vertical Slice Implementation Plan

Status: `design-only; implementation not authorized`

1. Require fresh P6A-C and P5D evidence.
2. Fixture one compatible governed ruleset parameter and isolated preview data;
   do not implement arbitrary executable package code.
3. Execute reader/editor/admin flows through UI, CLI and MCP contract fixtures,
   including denial and rollback.
4. Add security-focused Harness evidence for audit, redaction, active-revision
   replay and zero raw-fact write.

P7 research adapters may begin only after these governance gates are green.
