# INF-1Z Ecology Frost State-Action Plan

Status: `implemented and independently verified for the fixed Ecology frost dispel row; broader INF-1 remains incomplete`

1. Verify the existing frost owner, canonical stream, open-obligation identity,
   project projection and checkpoint-tail replay reader. `completed`
2. Add focused RED tests for the fixed semantic proposal -> Ecology dispel and
   its success, zero-write, idempotency, privacy and replay boundaries.
   `completed`
3. Add the immutable lifecycle action metadata and owner-side two-event fragment.
   `completed`
4. Route only the fixed semantic proposal through the existing Ecology owner
   command/envelope/settlement-plan/append spine. `completed`
5. Add a dedicated Harness profile/report and synchronize August, formal and
   plan indexes; run predecessor profiles, `git diff --check` and full pytest.
   `completed`

Non-goals: generic action registration, crop repair or transform, arbitrary
Ecology semantic actions, generic lifecycle dispatch, other consumer edges,
and complete group simulation.
