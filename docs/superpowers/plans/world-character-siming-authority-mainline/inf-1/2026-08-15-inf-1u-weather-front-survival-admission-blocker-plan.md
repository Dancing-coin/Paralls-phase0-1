# INF-1U Weather-front Survival Admission Blocker Plan

Status: `superseded for the exact weather:frost cold row; broader admission remains blocked`

Date: `2026-08-15`

**Goal:** Keep `weather-front -> Survival` unsupported-input zero-write until a
formal existing Survival-owner contract is approved. INF-4AC has supplied only
the bounded event-derived target mapping prerequisite.

1. Add focused tests proving the Survival lifecycle contract matrix remains
   closed to the four existing rows.
2. Add focused tests proving weather-front-shaped provenance cannot enter the
   Survival apply bridge or the existing Survival action route.
3. Add a dedicated verifier/profile and evidence report for this blocker.
4. Update the INF-1 spec/plan tree and August INF status so the blocker stays
   visible instead of being mistaken for an unimplemented row.

## Stop condition

Stop at blocked status unless a future approved package names all of the
following for a weather-front -> Survival row:

- existing owner;
- canonical source event family;
- the existing INF-4AC event-derived `region_ref -> profile_ref` target
  projection with source revision and project privacy; profile homeland, local
  client position and household `residence_ref` are not substitutes;
- target stream pattern;
- scoped projection/privacy;
- revision/idempotency rule; and
- append-derived receipt/replay contract.
