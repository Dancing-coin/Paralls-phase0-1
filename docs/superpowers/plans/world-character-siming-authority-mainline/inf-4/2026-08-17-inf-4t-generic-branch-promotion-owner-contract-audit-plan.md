# INF-4T Generic Branch Promotion Owner-Contract Audit Plan

Status: `blocked after bounded branch-work-to-wage promotion audit`

1. Keep generic branch settlement, promotion, and group simulation
   unimplemented. In particular, retain `BranchPreviewAuthority.promote()`
   zero-write for a `work` candidate.
2. Preserve isolated creator-debug branch replay/evidence, the actor-scoped
   Production-to-Economy wage consumer, and the two exact Government/
   Organization promotions as separate rows. Do not use a branch candidate as
   completed Production evidence.
3. Before any branch-work implementation, write and obtain approval for one
   source-specific branch-to-Economy Owner-Admission Contract. It must fix the
   Production evidence source, Economy command/event stream/revisions,
   creator-debug-to-actor privacy boundary, idempotency, append receipt,
   isolation-preserving full/tail replay, and correction/compensation rule.
4. Only after that approval, write focused RED tests and an independent Harness
   profile for success, zero-write rejection, privacy, revision, idempotency,
   receipt, full/checkpoint-tail replay, and the exact terminal or
   compensation boundary.
