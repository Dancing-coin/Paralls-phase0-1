# Adventure-Basic Reference Closure Plan

Status: `implemented-and-verified; all five governed reference scenarios have backend, replay, mirror, explanation, and real-Godot evidence`

## Execution Update (2026-08-04)

The five scoped scenarios are complete for this reference closure: purchase
and equip sword; injury/insufficient-stamina constraint; storage-ring
container and carried weight; land-right versus physical deed; and
gift/debt/typed-contract lifecycle. The fresh
`.harness/verification/adventure-basic-report.json` proves the governed
manifest, per-scenario rebuilt facade with revision/result metadata, online,
full, and checkpoint-tail canonical replay hashes, filtered mirror source, and
real Godot delivery after the canonical authority commit. It does not promote
Patch activation, client authority, generic transport durability, dynamic
market, cultivation, or relationship-graph work into this closure.

## Current Baseline

`assets/gameplay/adventure-basic/manifest.json` provides a strict,
digest-checked governed package entry, and `adventure-basic` verifies it before
the reference commands run. The five scoped compositions reuse existing
authority services and prove both authoritative success and structured failure
without relaxing their domain boundaries. Patch activation remains deliberately
outside the reference closure.

## Dependencies

All preceding first-closure plans, including Rule IR/capabilities, persistence,
and Godot mirror work.

## Work

1. [x] Author and validate the `adventure-basic` manifest, seeds,
   item/container fixtures, fixed offers, actor/account fixtures, and Godot
   bindings for the five reference scenarios.
2. [x] Prove purchase/equip sword; injury and stamina rejection; storage ring
   encumbrance; land right versus deed; and gift/debt/contract lifecycle with
   backend, replay, mirror, explanation, and real-Godot evidence.
3. For each scenario retain command/result, event batch, revisions, rebuilt
   facade, mirror output, explanation trace, replay hash, and Godot evidence.
4. [x] Run all dependency profiles and `gameplay-foundation-all`; the historic
   repository-wide run reaches the later fail-closed physical-door gate, whose
   separate status is recorded in its vertical-slice plan.

## Exit Criteria

Every scenario has an authoritative success or structured constraint result,
replays identically, and produces the specified observable Godot result. The
closure does not claim deferred cultivation, dynamic market, relationship graph,
or Siming knowledge graph implementation.
