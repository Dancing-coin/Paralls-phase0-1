# INF-4AI P5 Actor-Private Expression Amendment Plan

Status: `implemented narrow platform extension for INF-4AI; generic expansion remains blocked`

1. Freeze the exact static vocabulary decision: one P5 schema event, one
   stream grammar, one `actor_private` catalog scope, one descriptor, and one
   Social owner operation. Reject any general session/social abstraction.
2. Write a file-by-file implementation plan that modifies only existing P5
   registry/catalog/event-schema/Social owner boundaries. It must preserve all
   existing P5 revisions and fail closed for unknown vocabulary.
3. Prepare the non-admitted immutable P5 handshake package content packet,
   including author/trust, definition/schema, declaration/binding/predicate/
   effect, reader/profile, canonical ordering, and untrusted digest claims.
4. After content approval, run existing v2 adapter digest validation and the
   existing candidate/active binding lifecycle. Do not alter any frozen
   package revision.
5. Descriptor/catalog and exact owner runtime are now implemented only for
   this row; the focused RED-to-green suite and independent Harness are green.
6. Verify source vector, zero-write/privacy, duplicate/changed-duplicate,
   append-derived receipt, full replay, checkpoint-tail replay, and all
   predecessor P5/embodied tests before recording the row as implemented.

Evidence: dedicated INF-4AI suite `8 passed`; P5 Social and catalog regression
suite `31 passed`; independent Harness
`overall_inf4ai_p5_actor_private_expression_passed=True`. No package freeze,
generic social writer, new owner, or second registry/runtime was introduced.
