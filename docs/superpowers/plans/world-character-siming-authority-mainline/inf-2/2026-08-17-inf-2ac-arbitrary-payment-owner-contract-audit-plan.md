# INF-2AC Arbitrary Payment Owner-Contract Audit Plan

Status: `generic arbitrary payment remains owner-contract blocked; package-declared negotiated-exchange row implemented narrowly`

1. Preserve rejection for caller-open policy/payment requests.
2. Do not create treasury, account, generic settlement, compensation, or
   coordinator layers.
3. Treat INF-2AB as one non-generalizable tax recipe, not a reusable payment
   owner. The separately approved `package_declared_negotiated_exchange@1`
   contract has completed its focused tests, Harness, and existing-Economy
   append vertical; resume only with a new, independently approved row-specific
   contract.
