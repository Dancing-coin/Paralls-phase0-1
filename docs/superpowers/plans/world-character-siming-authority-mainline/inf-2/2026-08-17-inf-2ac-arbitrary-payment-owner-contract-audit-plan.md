# INF-2AC Arbitrary Payment Owner-Contract Audit Plan

Status: `candidate approved; complete owner-admission contract awaiting explicit row approval`

1. Preserve rejection for caller-open policy/payment requests.
2. Do not create treasury, account, generic settlement, compensation, or
   coordinator layers.
3. Treat INF-2AB as one non-generalizable tax recipe, not a reusable payment
   owner. The bounded `package_declared_negotiated_exchange@1` contract and
   approval-gated plan now define the next candidate. Resume with RED tests
   only after explicit approval of that complete contract.
