# INF-4 Owner-Admission Candidate Register

Status: `INF-4AK implemented narrow Government acknowledgment vertical; branch/population remainder retains formal blockers`

## Shared Contract Requirements

Branch preview is creator-debug evidence only. A candidate must cite committed
branch/Production evidence, a fixed existing owner and exact consequence. The
owner selects stream, privacy, idempotency, receipt, compensation and fixed
event vector. Branch and production histories remain separate; full and
checkpoint-tail replay must be independently equal. Unknown, multiple,
unadmitted, digest/privacy/stale evidence, binding/revision conflict, duplicate
and changed duplicate are zero-write.

## Candidate INF-4-SLOT-A (committed branch/Production evidence -> owner consequence; blocked)

The remaining audit has no new exact evidence/consequence pair beyond the
fixed Government/Organization promotions and the branch-work wage row.
Missing evidence event/view and revision, owner/stream/event/write revision,
privacy and subject binding, idempotency, receipt, replay, terminal/correction/
compensation, and package/declaration/binding/policy/descriptor/catalog pins.
Recommendation: approve one typed consequence and its source owner first.

The 2026-08-26 municipal assessment Contract/Economy chain is not an INF-4
branch consequence. It has no branch-local source and cannot be relabelled as
one. The remaining INF-4 requirement is still a distinct committed
Production/domain source -> exact target-owner outcome pairing.

## Candidate INF-4-SLOT-B (branch work -> wage; existing reference, not new fact)

The branch-work wage contract is already a narrow implemented row. A branch
candidate cannot be re-used as Production truth or generic promotion. No new
contract is formed; preserve the exact existing source/evidence fence and
zero-write for branch-only requests.

## Candidate INF-4-SLOT-C (inspection/supply sibling; existing references)

Government passed/failed inspection and Organization supply promotions are
already fixed rows. Names or fixtures cannot create another social, population,
or promotion fact. Missing a new committed evidence/outcome decision means
owner-contract blocked; no generic promotion registry or branch settlement
writer is permitted.

## Candidate INF-4U (implemented narrow vertical)

The exact fulfilled INF-3S municipal assessment service contract now grants one
fixed, authority-only Ownership certificate title to
`organization:district-works`. The right and asset are derived only from the
advisory-derived contract id; no caller chooses them. Focused source/revision,
idempotency, append receipt, full/tail replay and zero-write coverage is
recorded in the [contract](2026-08-26-inf-4u-municipal-drought-assessment-certificate-owner-admission-design.md)
and `inf4u-municipal-drought-assessment-certificate` Harness. It is not a
branch consequence, payment, inspection result, social fact or generic
certificate authority.

## Candidate INF-4AK (implemented narrow vertical)

The exact INF-4AJ project-visible `funded_and_executed` record, its fixed
INF-2AI consumed-budget provenance, and acquisition-derived jurisdiction now
yield one authority-only Government administrative acknowledgment. This is not
a branch consequence, project completion, permit, certificate, payment,
material, output, attendance, social, population, or generic lifecycle fact.
Its separate contract, receipt and replay evidence remain fixed to this one
source vector.

## Remaining INF-4 Disposition

No further committed Production/domain evidence pairs with an unclaimed exact
existing-owner outcome. Branch requests remain creator-debug only; existing
work, supply, inspection and execution rows are closed references; population,
social and group truth retain their existing owner-contract blockers.
