# Explicitly Activated Art Packs

This directory is outside the default validation scene. Contents are copied
from `archive/` only for an explicit qualification or showcase run.

Do not add active pack references to `Unified3DIntegrationValidation.tscn`.
