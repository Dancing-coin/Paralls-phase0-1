# Player 操作指南

这份说明对应当前项目 `Paralls phase0 demo` 的实际输入映射。

主场景：

- `scenes/phase0/MainDemo.tscn`

## 基本移动

- `W` / `上方向键`：向前移动
- `S` / `下方向键`：向后移动
- `A` / `左方向键`：向左移动
- `D` / `右方向键`：向右移动
- `Shift`：奔跑
- `Space`：跳跃

## 镜头控制

- `鼠标移动`：旋转第三人称镜头
- `Ctrl`：切换鼠标捕获 / 释放
  - 进入场景后如果鼠标不能自由点界面，按一次 `Ctrl` 释放
  - 如果要继续用鼠标控制镜头，再按一次 `Ctrl` 重新捕获
- `鼠标右键`：进入 / 退出瞄准镜头
- `G`：切换肩部瞄准左右侧
- `鼠标滚轮上` 或 `V`：镜头拉近
- `鼠标滚轮下` 或 `B`：镜头拉远

## Phase 0 交互

- `F`：提交对话
  - 会向当前聚焦目标角色发送一条对话输入
- `E`：交互
  - 会向当前聚焦目标物体发送一次交互请求

## 当前聚焦规则

系统会自动从玩家前方选择一个聚焦目标。

可被聚焦的对象：

- `CharacterA`
- `CharacterB`
- `InteractiveObject`

聚焦判断主要看：

- 目标是否在玩家前方
- 距离是否在可聚焦范围内
- 朝向是否更接近镜头正前方

如果你按了 `F` 或 `E` 但没有反应，通常是以下原因之一：

- 当前前方没有合法目标
- 目标不在镜头前方
- 你离目标太远，系统没有把它选成当前聚焦对象

## 推荐操作流程

1. 进入场景后先用 `W/A/S/D` 移动，确认角色可控。
2. 用鼠标调整视角，把角色或桌上的物体放到玩家前方。
3. 面向角色时按 `F` 测试对话。
4. 面向桌上的信件时按 `E` 测试交互。
5. 用滚轮、`V`、`B` 和鼠标移动测试镜头远近与视角。

## 调试提示

- 屏幕左上角的文字是 `DebugOverlay` 输出，会显示：
  - 后端连接状态
  - 当前收到的对话/交互/环境变化/司命输出
- 如果自动演示开启，场景会自动执行一轮：
  - 对话
  - 交互
  - 环境变化
  - 司命反应

## 当前键位来源

这份操作指南来自当前项目中的实际输入配置：

- `project.godot`
- `scripts/player/Phase0PlayerBridge.gd`
- `addons/JehenoThirdPersonController/README.md`
